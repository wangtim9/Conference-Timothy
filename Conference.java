import java.io.*;
import java.util.*;

public class Conference {
	int numTables=10;
	int numCompanies;
	Attendee[150];
	int PplPerTable=10;
	public int Place(String guestfile) {
		File F = new File(file);
		Scanner scan = new Scanner(F);
		for (int i = 0; i<numTables; i++) {
			for (int j = 0; j<PplPerTable;j++) {
			
				if (
	
